# Registry Data Quality Audit

Known registry discrepancies caught during adapter testing.

| Date | Source | Issue | Action |
| --- | --- | --- | --- |
| 2026-05-22 | FRED | `requires_key` was `false`, but the FRED API requires an API key. Caught during Step 2 `official_api` adapter verification. | Corrected to `requires_key: true`, added `key_env_var: "FRED_API_KEY"`, and set `last_verified`. |
| 2026-05-22 | Reuters | Configured RSS feed URL returned `http_404` during Step 3 `rss` adapter verification. Reuters RSS candidates either returned `404` or anti-bot `401`; Reuters public news pages also triggered Firecrawl anti-bot failures. | Registry audit retagged source to `method: scrape`, changed `baseUrl` to the scrapeable Reuters Agency site, removed RSS feed fields, and set `last_verified`. |
| 2026-05-22 | Step 6 integration | Kept existing trust-to-reliability mapping (`medium_high` -> `medium`). Locked policy specified `medium_high` -> `high`, but this would change source reliability classifications across the registry. | Re-evaluate in a separate session after observing Step 6 in real queries. |
| 2026-05-22 | Hugging Face, Papers With Code, OpenReview, OpenRouter | Marked `method: official_api`, but `baseUrl` points at human-facing sites. Generic mapper cannot extract useful API data from HTML. | Registry audit added JSON endpoints for Hugging Face, OpenReview, and OpenRouter. Papers With Code API candidates redirected to HTML, so it was retagged to `method: scrape`. |
| 2026-05-22 | Anthropic, Mistral AI | RSS feeds returned `http_404` during Step 6 verification. Feed URLs have likely changed. | Registry audit found no working common RSS candidates. Retagged both to `method: scrape`, removed feed fields, and set `last_verified`. |
| 2026-05-22 | BBC | Entry was incorrectly carrying `requires_key: true` with `key_env_var: "FRED_API_KEY"`, even though BBC RSS is public. | Registry audit corrected BBC to `requires_key: false`. |
| 2026-05-22 | Papers With Code | Retagging to `scrape` works in isolation, but full parallel retrieval can hit the 10s adapter timeout because the homepage scrape is large. | Keep as scrape for now; improve in a later per-source pass with a narrower scrape URL or a source-specific endpoint if one becomes available. |
| 2026-05-22 | Reuters | Reuters Agency is scrapeable, but returned content includes cookie boilerplate and can still time out under full parallel load. | Keep the source enabled after removing dead RSS, but revisit in a per-source quality pass. |
| 2026-05-22 | Official API endpoint audit | CISA KEV, NVD, MITRE ATT&CK, Federal Register, URLhaus, Federal Reserve, ECB, NSE India, World Bank, IMF, BLS, BEA, OECD, CoinPaprika, DefiLlama, L2Beat, Coinbase API, and CryptoCompare had homepage-style API entries or missing endpoint paths. | Added verified endpoint URLs and hardened the generic API adapter for text/plain JSON, XML/plain fallbacks, wrapper arrays, and explicit query endpoints. |
| 2026-05-22 | Congress.gov, The Guardian, US Census | Public endpoints require API keys in current verification. | Marked `requires_key: true`, added `CONGRESS_API_KEY`, `GUARDIAN_API_KEY`, and `CENSUS_API_KEY`, and documented them in `.env.example`. Missing keys now degrade gracefully. |
| 2026-05-22 | Bank of England | The useful public source for current central-bank news is RSS, while the tested CSV/database feed does not fit the current JSON/Atom API adapter. | Retagged to `method: rss` using `https://www.bankofengland.co.uk/rss/news`. |
| 2026-05-22 | RBI, MalwareBazaar | RBI blocks direct API-style access from the current client; MalwareBazaar's useful API is POST/auth-oriented and does not fit the current GET/JSON adapter. | Retagged RBI and MalwareBazaar to `method: scrape` with reachable human-facing URLs. RBI uses the homepage because it scraped faster than the press-release listing under Firecrawl. |
| 2026-05-22 | Official API adapter | Endpoint-template matching incorrectly treated generic `api.*` hostnames as matching unrelated API templates, causing some sources to hit CoinPaprika accidentally. | Fixed matcher to ignore generic host prefixes and prefer explicit query endpoints before templates. |
| 2026-05-22 | Crypto endpoint pass | Binance, Kraken, Coinbase, CoinMarketCap, CoinGecko, and CryptoCompare needed cleaner market-data output. The Graph requires API-key-in-URL GraphQL and does not fit the current generic GET/JSON adapter. Token Terminal, Artemis, and Messari keys were not present in `.env` during this pass. | Added verified BTC market endpoints for Binance, Kraken, Coinbase, and CoinMarketCap; added readable mappers for crypto market responses; marked The Graph `requires_key: true` with `THE_GRAPH_API_KEY` and documented it as needing a future GraphQL adapter. |
| 2026-05-22 | Token Terminal, Artemis, Messari | These were incorrectly treated as free/key-gated `official_api` sources. Token Terminal API access requires a paid/custom API subscription; Messari's useful market/research/news API returned payment-required or anti-bot responses, with only limited MessariAI free calls; Artemis has free terminal/sheets/web access but the tested API endpoint is authenticated and pricing separates API access. A later Messari key check confirmed `MESSARI_API_KEY` loads, but the account receives `403` permission errors for permissions, news feed, asset list, and asset metrics endpoints. | Retagged Token Terminal and Artemis to `method: scrape`, `type: publisher`, `requires_key: false`. Retagged Messari to `scrape` but disabled it until the key has usable market/research permissions or a useful free, scrapeable content URL is confirmed. |
| 2026-05-22 | Scrape timeout pass | SEBI and Papers With Code homepages were too broad. Reuters remains cookie-wall heavy. RBI still has Firecrawl timeout variability under parallel load despite successful isolated scrapes. | Narrowed SEBI to a listing URL and Papers With Code to a language-modelling task page. Kept Reuters and RBI caveats for a future scrape/proxy-quality pass. |
| 2026-05-22 | Per-source mappers | NVD, CISA KEV, FRED, CoinGecko, CoinMarketCap, Binance, Kraken, Coinbase, CryptoCompare, World Bank, IMF, and BLS returned useful data but generic JSON snippets were hard for synthesis to use. | Added lightweight source-aware mappers inside the official API adapter to emit readable titles and concise fact summaries. |
| 2026-05-22 | US Census | The configured `CENSUS_API_KEY` was rejected as invalid during verification, so the source still cannot return data until the key is replaced or activated. | Adapter now classifies Census key failure as `auth_failed` instead of `non_json`; replace the key in `.env` before relying on Census data. |
| 2026-05-22 | Research pipeline latency | Full OpenAI deep pipeline was about 112s: retrieval about 10s, Hermes claim extraction + verification + synthesis about 102s combined. Real adapter content increased snippet payloads and Hermes stages dominate latency. | Pipeline optimization in separate session. Consider parallel Hermes stages, smaller snippet budgets, faster Hermes model, or two-stage retrieval with quality filtering. |
| 2026-05-23 | IPCC | Entry was incorrectly tagged as `method: official_api` even though `https://www.ipcc.ch` is HTML and the working freshness source is the site's RSS feed at `https://www.ipcc.ch/feed/`. | Retagged IPCC to `method: rss_plus_scrape`, changed `type` to `publisher`, added `feed_url` and `rssUrls`, and set `last_verified`. Adapter verification confirmed RSS parsing plus full-article scraping. |
| 2026-05-23 | Stanford HAI | Configured RSS URL `https://hai.stanford.edu/news/rss.xml` returned HTML instead of a valid feed, and alternate common feed URLs also returned HTML. | Retagged Stanford HAI to `method: scrape`, removed `feed_url` and `rssUrls`, kept the human site `baseUrl`, and set `last_verified`. Adapter verification confirmed usable scraped content. |
| 2026-05-23 | Stanford HAI | After retagging away from the broken feed, isolated scrape tests succeed, but the source can still hit the 10s scrape timeout under the full parallel AI soak run. | Keep the registry fix; any further improvement is a scrape-quality or timeout-tuning task for a separate session, not a registry data issue. |

## Deferred To Adapter Work

| Date | Source | Current State | Blocker | Needed |
| --- | --- | --- | --- | --- |
| 2026-05-23 | The Graph | Registry data is already correct: `requires_key: true`, `key_env_var: "THE_GRAPH_API_KEY"`, and adapter dispatch reaches the source. | The source is GraphQL, not REST. The generic `official_api` adapter issues GET-style requests with query params, but The Graph needs POST with a GraphQL query body and its response shape differs from the generic JSON mapper. | Add a small `graphql.ts` adapter or a per-source `official_api` override for POST-with-query-body plus GraphQL response extraction. Estimated effort: about 60-100 lines. |
| 2026-05-23 | OpenWeatherMap | Registry endpoint and auth are correct, and raw HTTP succeeds for `q=Mumbai`. | Natural-language queries arrive as phrases like `weather in Mumbai`; the current adapter passes the whole phrase into `q=`, and OpenWeatherMap returns `404 city not found`. | Add a per-source query transformer or enrich topic/entity extraction so location entities are isolated before adapter dispatch. A per-source transformer is about 30 lines; extractor enrichment is broader but reusable. |
| 2026-05-23 | Papers With Code | Source is already retagged to `method: scrape`, and the current page scrapes successfully. | The configured page returns generic trending content regardless of query, and historical API candidates now return HTML instead of structured JSON. | Investigate either query-aware scrape URL construction, a confirmed current API endpoint, or keep the current best-effort behavior. This likely needs per-source query construction logic. |
